import { Typography } from "@mui/material";
import { StyledFooter } from "./styles";

const Footer = () => {
  return (
    <StyledFooter>
      <Typography variant="body2">Esto es el Footer &copy; Irene </Typography>
    </StyledFooter>
  );
};

export default Footer;
